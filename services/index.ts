// Unified Services Export
// All mode-dependent logic is abstracted here

export { uiConfig, type UIConfig } from './ui';
export { authService, type AuthService } from './auth';

// AI service is accessed via existing pattern
// import { claudeProvider } from './services/ai/claude';
